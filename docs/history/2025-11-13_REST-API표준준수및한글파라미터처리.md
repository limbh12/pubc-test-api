# REST API 표준 준수 및 한글 파라미터 처리 개선

**작업일**: 2025년 11월 13일  
**커밋**: a7fedbb  
**작업자**: GitHub Copilot

---

## 📋 작업 개요

JBoss 로컬 배포 및 API 테스트 중 발견된 두 가지 주요 문제를 해결:
1. 존재하지 않는 리소스 조회 시 HTTP 400 반환 (REST 표준 위반)
2. 한글 파라미터 URL 인코딩 누락으로 인한 400 에러 및 로그 미기록

---

## 🔍 문제 분석

### 문제 1: REST API 표준 위반 (HTTP 404 vs 400)

**발견 경위**:
```bash
# 테스트 스크립트 실행 시
9.3 존재하지 않는 시설 ID:
   ⚠ 예상과 다른 응답 (HTTP 400)
```

**문제점**:
- 존재하지 않는 시설 ID 조회 시 HTTP 400 Bad Request 반환
- REST API 표준에서는 HTTP 404 Not Found가 적절
- 로그에서 ERROR 레벨로 기록 (과도한 경고)

**원인 분석**:
```java
// CultureFacilityServiceImpl.java
if (facility == null) {
    throw new IllegalArgumentException("해당 시설을 찾을 수 없습니다: " + facilityId);
    // ❌ IllegalArgumentException → HTTP 400
}

// CultureFacilityRestService.java
} catch (IllegalArgumentException e) {
    // ❌ 모든 IllegalArgumentException을 400으로 처리
    return Response.status(Response.Status.BAD_REQUEST).entity(errorResponse).build();
}
```

### 문제 2: 한글 파라미터 처리 오류

**발견 경위**:
```bash
curl "http://localhost:8080/pubc-test-api/api/facilities?serviceKey=TEST_KEY_001&facilityType=박물관"
# HTTP 400 Bad Request
# 로그에 아무런 기록 없음 ❌
```

**문제점**:
- 한글 파라미터를 URL 인코딩 없이 전송 시 HTTP 400 에러
- 애플리케이션 로직에 도달하지 못해 로그 미기록
- 테스트 스크립트와 문서에 URL 인코딩 가이드 부재

---

## 🛠️ 해결 방법

### 1. ResourceNotFoundException 예외 클래스 생성

**파일**: `src/main/java/iros/test/common/exception/ResourceNotFoundException.java`

```java
package iros.test.common.exception;

/**
 * 리소스를 찾을 수 없을 때 발생하는 예외
 * HTTP 404 Not Found 응답에 사용
 */
public class ResourceNotFoundException extends RuntimeException {
    
    public ResourceNotFoundException() {
        super("요청한 리소스를 찾을 수 없습니다");
    }
    
    public ResourceNotFoundException(String message) {
        super(message);
    }
    
    public ResourceNotFoundException(String message, Throwable cause) {
        super(message, cause);
    }
}
```

**설계 의도**:
- HTTP 404 전용 예외 클래스
- `IllegalArgumentException`과 구분하여 명확한 의미 전달
- REST API 표준 준수

### 2. 서비스 레이어 수정

**파일**: `src/main/java/iros/test/facility/service/CultureFacilityServiceImpl.java`

```java
// Before
if (facility == null) {
    logger.warn("문화시설을 찾을 수 없습니다 - facilityId: {}", facilityId);
    throw new IllegalArgumentException("해당 시설을 찾을 수 없습니다: " + facilityId);
}

// After
if (facility == null) {
    logger.warn("문화시설을 찾을 수 없습니다 - facilityId: {}", facilityId);
    throw new ResourceNotFoundException("해당 시설을 찾을 수 없습니다: " + facilityId);
}
```

**변경 사항**:
- `IllegalArgumentException` → `ResourceNotFoundException`
- 예외 타입만 변경, 메시지와 로그는 동일 유지

### 3. 컨트롤러 예외 처리 추가

**파일**: `src/main/java/iros/test/facility/controller/CultureFacilityRestService.java`

```java
// 새로운 catch 블록 추가
} catch (ResourceNotFoundException e) {
    logger.warn("리소스를 찾을 수 없음: {}", e.getMessage());
    
    Map<String, Object> errorResponse = new HashMap<>();
    errorResponse.put("resultCode", "04");  // 새로운 에러 코드
    errorResponse.put("resultMessage", e.getMessage());
    
    return Response.status(Response.Status.NOT_FOUND).entity(errorResponse).build();
    
} catch (IllegalArgumentException e) {
    // 기존 처리 유지 (HTTP 400)
    ...
}
```

**변경 사항**:
- `ResourceNotFoundException` 전용 catch 블록 추가
- HTTP 404 Not Found 반환
- 로그 레벨: ERROR → WARN (정상적인 비즈니스 로직)
- 새로운 에러 코드 `04` 할당

### 4. 테스트 스크립트 수정 (한글 파라미터 처리)

**파일**: `scripts/test-api.sh`

```bash
# Before (❌ HTTP 400 에러)
curl -s "${BASE_URL}/facilities?serviceKey=${SERVICE_KEY}&facilityType=박물관&pageSize=3"

# After (✅ URL 인코딩)
curl -s -G "${BASE_URL}/facilities" \
  --data-urlencode "serviceKey=${SERVICE_KEY}" \
  --data-urlencode "facilityType=박물관" \
  --data-urlencode "pageSize=3"
```

**추가 수정**:
- API 응답 구조 업데이트: `items` → `data`, `code` → `resultCode`
- 상세 조회 시설 ID: `FAC001` → `F001` (실제 Mock 데이터와 일치)
- 필드명 수정: `phone` → `phoneNumber`, `homepage` → `website`

### 5. 문서 업데이트

**파일**: `docs/08_로컬배포및API테스트가이드.md`

#### 5.1 주요 경로 정보 추가 (섹션 4.0)

```markdown
### 4.0 주요 경로 정보

#### 로그 파일 경로
📋 /Users/byunglim/Prj_Claude/PubcTestApi/logs/server.log
- 설정 위치: env/jboss/wildfly-26.1.3.Final/standalone/configuration/logging.properties
- 로그 로테이션: 날짜별 (.yyyy-MM-dd suffix)
- 로그 레벨: ALL

#### WAR 배포 및 압축 해제 경로
1. 원본 WAR: env/jboss/.../standalone/deployments/pubc-test-api.war
2. 실제 배포 컨텐츠: env/jboss/.../standalone/data/content/27/.../content
3. 임시 컴파일: env/jboss/.../standalone/tmp/pubc-test-api.war/
```

#### 5.2 한글 파라미터 처리 가이드 추가

```markdown
### 5.1 테스트 준비

**중요 사항**:
- ⚠️ **한글 파라미터는 반드시 URL 인코딩 필요**
- 권장: `curl -G` + `--data-urlencode` 옵션 사용
- 인코딩 없이 전송 시 HTTP 400 에러 발생 및 로그 미기록

**시설 유형별 조회 (박물관)**:
```bash
# 방법 1: --data-urlencode 사용 (권장)
curl -G "http://localhost:8080/pubc-test-api/api/facilities" \
  --data-urlencode "serviceKey=TEST_KEY_001" \
  --data-urlencode "facilityType=박물관"

# 방법 2: 수동 URL 인코딩
curl "http://localhost:8080/pubc-test-api/api/facilities?serviceKey=TEST_KEY_001&facilityType=%eb%b0%95%eb%ac%bc%ea%b4%80"
```

> **⚠️ 중요**: 한글 파라미터는 반드시 URL 인코딩이 필요합니다!
```

#### 5.3 문제 해결 섹션 강화 (6.4)

```markdown
### 6.4 인증 오류

**한글 파라미터 처리 문제**:

❌ 잘못된 방법 (HTTP 400 에러, 로그 미기록)
curl "...?facilityType=박물관"

✅ 올바른 방법 1: --data-urlencode 사용
curl -G --data-urlencode "facilityType=박물관"

✅ 올바른 방법 2: 수동 URL 인코딩
curl "...?facilityType=%eb%b0%95%eb%ac%bc%ea%b4%80"
```

---

## ✅ 검증 결과

### 1. HTTP 404 처리 검증

```bash
# 존재하지 않는 ID 조회
$ curl -s "http://localhost:8080/pubc-test-api/api/facilities/INVALID_ID?serviceKey=TEST_KEY_001" | jq '.'
{
  "resultCode": "04",
  "resultMessage": "해당 시설을 찾을 수 없습니다: INVALID_ID"
}

# HTTP 상태 코드 확인
$ curl -s -o /dev/null -w "HTTP %{http_code}\n" "http://localhost:8080/.../INVALID_ID?serviceKey=..."
HTTP 404  ✅
```

**로그 확인**:
```
2025-11-13 17:08:16,912 WARN  [CultureFacilityServiceImpl] 문화시설을 찾을 수 없습니다 - facilityId: INVALID_ID
2025-11-13 17:08:16,915 WARN  [CultureFacilityRestService] 리소스를 찾을 수 없음: 해당 시설을 찾을 수 없습니다: INVALID_ID
```
- ✅ ERROR → WARN 변경
- ✅ HTTP 404 정상 반환

### 2. 한글 파라미터 처리 검증

```bash
# URL 인코딩 사용
$ curl -s -G "http://localhost:8080/pubc-test-api/api/facilities" \
  --data-urlencode "serviceKey=TEST_KEY_001" \
  --data-urlencode "facilityType=박물관" \
  --data-urlencode "pageSize=3" | jq '.totalCount'
2  ✅

# 로그 확인
2025-11-13 16:56:41,499 INFO [MockCommonProc]
  - Query String: serviceKey=TEST_KEY_001&facilityType=%eb%b0%95%eb%ac%bc%ea%b4%80&pageSize=3
  ✅ 로그 정상 기록
```

### 3. 통합 테스트 결과

```bash
$ ./scripts/test-api.sh

9. 에러 케이스 테스트
   9.1 잘못된 서비스키:
      ✓ 예상대로 거부됨 (HTTP 400)
   9.2 서비스키 누락:
      ✓ 예상대로 거부됨 (HTTP 400)
   9.3 존재하지 않는 시설 ID:
      ✓ 예상대로 404 반환 (HTTP 404)  ⭐ 수정 완료!
   9.4 잘못된 페이지 번호:
      ✓ 예상대로 400 반환 (HTTP 400)

11개 테스트 모두 통과 ✅
평균 응답 시간: 13ms (성능 우수)
```

---

## 📊 변경 통계

### Git 변경 사항
```
커밋: a7fedbb
변경 파일: 5개
추가: 245줄
삭제: 30줄
순증가: 215줄
```

### 파일별 변경

| 파일 | 변경 유형 | 변경 내용 |
|------|-----------|-----------|
| `ResourceNotFoundException.java` | 신규 | HTTP 404 전용 예외 클래스 (39줄) |
| `CultureFacilityServiceImpl.java` | 수정 | IllegalArgumentException → ResourceNotFoundException (3줄) |
| `CultureFacilityRestService.java` | 수정 | 404 예외 처리 추가 (10줄) |
| `test-api.sh` | 수정 | 한글 파라미터 URL 인코딩, 응답 구조 업데이트 (45줄) |
| `08_로컬배포및API테스트가이드.md` | 수정 | 주요 경로, 한글 파라미터 가이드 추가 (178줄) |

---

## 🎯 주요 개선 사항

### 1. REST API 표준 완전 준수

**HTTP 상태 코드 매핑**:
| 상황 | Before | After | 비고 |
|------|--------|-------|------|
| 리소스 없음 | 400 ❌ | 404 ✅ | REST 표준 |
| 잘못된 요청 | 400 | 400 | 유지 |
| 서버 오류 | 500 | 500 | 유지 |

**에러 코드 체계**:
- `00`: 성공
- `01`: 잘못된 요청 (HTTP 400)
- `04`: 리소스 없음 (HTTP 404) ⭐ 신규
- `99`: 서버 오류 (HTTP 500)

### 2. 한글 파라미터 완벽 지원

**Before**:
```bash
❌ curl "...?facilityType=박물관"  # HTTP 400, 로그 없음
```

**After**:
```bash
✅ curl -G --data-urlencode "facilityType=박물관"  # HTTP 200, 로그 기록
```

### 3. 예외 처리 구조 개선

```
ResourceNotFoundException → HTTP 404 (리소스 없음)
IllegalArgumentException → HTTP 400 (잘못된 요청)
Exception → HTTP 500 (서버 오류)
```

### 4. 로그 레벨 최적화

**리소스 없음 처리**:
```
Before: ERROR [잘못된 요청: 해당 시설을 찾을 수 없습니다]
After:  WARN  [리소스를 찾을 수 없음: 해당 시설을 찾을 수 없습니다]
```
- 정상적인 비즈니스 로직이므로 WARN 레벨이 적절

---

## 📝 향후 개선 사항

### 1. 에러 응답 표준화
현재 각 에러마다 다른 구조를 사용하고 있어, 통일된 에러 응답 포맷 고려 필요:

```java
// 표준 에러 응답 객체
public class ErrorResponse {
    private String resultCode;
    private String resultMessage;
    private String timestamp;
    private String path;
    // ...
}
```

### 2. 예외 처리 공통화
컨트롤러마다 중복된 예외 처리 코드 → `@ExceptionHandler` 사용 고려:

```java
@ControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(ResourceNotFoundException.class)
    public Response handleResourceNotFound(ResourceNotFoundException e) {
        // 통합 404 처리
    }
}
```

### 3. URL 인코딩 자동화
클라이언트 라이브러리 제공 시 URL 인코딩 자동 처리 고려

---

## 📚 참고 자료

- **REST API HTTP 상태 코드**: https://restfulapi.net/http-status-codes/
- **URL 인코딩 표준**: RFC 3986
- **JAX-RS 예외 처리**: JSR 339 Specification
- **Spring Exception Handling Best Practices**

---

**작업 완료**: 2025년 11월 13일 17:10  
**테스트 통과**: 11/11 (100%)  
**성능**: 평균 응답 시간 13ms
