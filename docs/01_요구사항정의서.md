# 요구사항 정의서
## PUBC 연동 테스트 프로젝트

**작성일**: 2025-11-03
**프로젝트명**: PUBC-Test-API
**버전**: 2.0 (JDK 1.8)

> **📌 기술 스택**: 이 문서는 **[JDK 1.8 기술 스택](./00_기술스택_JDK18.md)** 기준으로 작성되었습니다.
> - Java: JDK 1.8
> - Spring: 4.3.30.RELEASE
> - Apache CXF: 3.3.13
> - MyBatis: 3.5.13 (프로젝트용)
> - PUBC 모듈: Ver.2.3 (iBATIS 2.3.4 포함, 완벽 호환)

---

## 1. 프로젝트 개요

### 1.1 목적
PUBC 공통연계모듈(iros_pubc_1.1.jar)을 활용하여 공공데이터 OpenAPI의 표준 인증 및 로깅 기능을 구현한 테스트 프로젝트를 개발합니다.

### 1.2 범위
- PUBC 모듈을 활용한 서비스키 기반 인증
- 사용 현황 및 장애 정보 자동 로깅
- REST API 서비스 구현 (조회 전용)
- SOAP 웹 서비스 구현 (조회 전용)
- 공공데이터 조회 기능 제공 (문화시설 정보)

### 1.3 참조 문서
- OASIS.md: OpenAPI 자동생성툴 분석
- PUBC.md: 공통연계모듈 상세 분석
- CLAUDE.md: 전체 시스템 아키텍처

---

## 2. 기능 요구사항

### 2.1 인증 기능 (FR-AUTH)

#### FR-AUTH-001: 서비스키 인증
- **설명**: HTTP 요청의 서비스키(serviceKey)를 검증하여 API 접근 권한 확인
- **입력**: HTTP Request (Query Parameter 또는 Header)
  - `serviceKey`: 발급된 서비스 키
- **처리**:
  - PUBC `UserCrtfcProcessService.userCrtfcProcess()` 호출
  - `UserCrtfcVO` 반환값의 `troblTyCode` 확인
- **출력**:
  - 정상: `troblTyCode = "00"` → 비즈니스 로직 실행
  - 오류: `troblTyCode = "30"` → 서비스키 미등록 오류 반환
- **에러 코드**:
  - `00`: 정상
  - `30`: SERVICE_KEY_IS_NOT_REGISTERED_ERROR
  - `31`: DEADLINE_HAS_EXPIRED_ERROR
  - `32`: UNREGISTERED_IP_ERROR
  - `33`: UNSIGNED_CALL_ERROR

#### FR-AUTH-002: IP 검증
- **설명**: 등록된 IP 주소에서만 API 호출 허용
- **입력**: 클라이언트 IP 주소
- **처리**: PUBC 인증 모듈 내부에서 IP 검증
- **출력**: IP 불일치 시 `troblTyCode = "32"` 반환

#### FR-AUTH-003: 사용량 임계값 검증
- **설명**: 일일/월간 사용량 제한 확인
- **입력**: 서비스키별 누적 호출 건수
- **처리**: PUBC 임계값 관리 모듈
- **출력**: 임계값 초과 시 `troblTyCode = "22"` 반환

### 2.2 로깅 기능 (FR-LOG)

#### FR-LOG-001: 사용 현황 로깅
- **설명**: 모든 API 호출에 대해 사용 현황 자동 기록
- **입력**: `UserCrtfcVO` 객체
- **처리**: `UseSttusService.insertUseSttus()` 호출
- **출력**: `TN_PUBC_USE_STTUS_INFO` 테이블에 저장
- **기록 항목**:
  - 서비스키, 호출 시각
  - 요청 파라미터
  - 응답 코드
  - 처리 시간
  - 클라이언트 IP

#### FR-LOG-002: 장애 정보 로깅
- **설명**: 인증 실패 또는 예외 발생 시 장애 정보 기록
- **입력**: 오류 코드, 오류 메시지
- **처리**: `UserCrtfcVO.setTroblTyCode()` 설정 후 로깅
- **출력**: 장애 유형 코드와 상세 내용 저장

#### FR-LOG-003: 통계 정보 제공
- **설명**: 로깅 데이터를 기반으로 통계 정보 조회 (선택사항)
- **입력**: 조회 기간, 서비스 ID
- **출력**: 호출 건수, 성공률, 평균 응답 시간

### 2.3 REST API 기능 (FR-REST)

#### FR-REST-001: 문화시설 목록 조회 API
- **엔드포인트**: `GET /api/cultureFacilities`
- **파라미터**:
  - `serviceKey` (필수): 서비스키
  - `facilityType` (선택): 시설 유형 (박물관, 미술관, 도서관 등)
  - `regionCode` (선택): 지역 코드 (11:서울, 26:부산 등)
  - `facilityName` (선택): 시설명 검색 (부분일치)
  - `pageNo` (선택): 페이지 번호 (기본값: 1)
  - `numOfRows` (선택): 페이지당 건수 (기본값: 10)
- **응답 포맷**: JSON
```json
{
  "code": "000",
  "msg": "OK",
  "data": {
    "totalCount": 150,
    "pageNo": 1,
    "numOfRows": 10,
    "items": [
      {
        "facilityId": "F001",
        "facilityName": "국립중앙박물관",
        "facilityType": "박물관",
        "address": "서울특별시 용산구 서빙고로 137",
        "phone": "02-2077-9000",
        "regionCode": "11"
      }
    ]
  }
}
```
- **에러 응답**:
```json
{
  "code": "030",
  "msg": "서비스키가 등록되지 않았습니다.",
  "data": null
}
```

#### FR-REST-002: 문화시설 상세 조회 API
- **엔드포인트**: `GET /api/cultureFacilities/{facilityId}`
- **파라미터**:
  - `serviceKey` (필수): 서비스키
  - `{facilityId}` (Path): 시설 ID
- **응답 포맷**: JSON (단일 객체, 상세 정보 포함)
```json
{
  "code": "000",
  "msg": "OK",
  "data": {
    "facilityId": "F001",
    "facilityName": "국립중앙박물관",
    "facilityType": "박물관",
    "address": "서울특별시 용산구 서빙고로 137",
    "phone": "02-2077-9000",
    "latitude": 37.5240,
    "longitude": 126.9803,
    "openTime": "10:00-18:00 (월요일 휴관)",
    "homepage": "https://www.museum.go.kr",
    "regionCode": "11",
    "manageAgency": "문화체육관광부",
    "updateDate": "2025-01-15"
  }
}
```

### 2.4 SOAP 웹 서비스 기능 (FR-SOAP)

#### FR-SOAP-001: 문화시설 목록 조회 서비스
- **서비스명**: CultureFacilityService
- **메서드**: `getCultureFacilityList`
- **입력 파라미터**:
  - `serviceKey` (string): 서비스키
  - `facilityType` (string, optional): 시설 유형
  - `regionCode` (string, optional): 지역 코드
  - `facilityName` (string, optional): 시설명
  - `pageNo` (int, optional): 페이지 번호
  - `numOfRows` (int, optional): 페이지당 건수
- **출력**: CultureFacilityList (배열)
- **WSDL 생성**: 자동 생성 지원

#### FR-SOAP-002: 문화시설 상세 조회 서비스
- **메서드**: `getCultureFacility`
- **입력**: serviceKey, facilityId
- **출력**: CultureFacility (단일 객체, 상세 정보 포함)

---

## 3. 비기능 요구사항

### 3.1 성능 요구사항 (NFR-PERF)
- **NFR-PERF-001**: API 평균 응답 시간 1초 이내
- **NFR-PERF-002**: 동시 접속 사용자 100명 지원
- **NFR-PERF-003**: TPS(Transaction Per Second) 50 이상

### 3.2 보안 요구사항 (NFR-SEC)
- **NFR-SEC-001**: 모든 API는 서비스키 인증 필수
- **NFR-SEC-002**: SQL Injection 방어 (PreparedStatement 사용)
- **NFR-SEC-003**: XSS 방어 (입력값 검증)
- **NFR-SEC-004**: 민감 정보 로깅 제외 (비밀번호 등)

### 3.3 가용성 요구사항 (NFR-AVAIL)
- **NFR-AVAIL-001**: 시스템 가동률 99% 이상
- **NFR-AVAIL-002**: 장애 발생 시 자동 로깅 및 알림

### 3.4 확장성 요구사항 (NFR-SCALE)
- **NFR-SCALE-001**: 새로운 API 추가 용이
- **NFR-SCALE-002**: 다양한 데이터 소스 연동 가능
- **NFR-SCALE-003**: PUBC 모듈 버전 업그레이드 대응

### 3.5 호환성 요구사항 (NFR-COMPAT)
- **NFR-COMPAT-001**: Java 5/6 호환성 유지
- **NFR-COMPAT-002**: PUBC Ver.2.3 호환
- **NFR-COMPAT-003**: Spring Framework 2.5.6 호환
- **NFR-COMPAT-004**: Apache CXF 2.2.9+ 호환

### 3.6 유지보수 요구사항 (NFR-MAINT)
- **NFR-MAINT-001**: 코드 주석 및 문서화
- **NFR-MAINT-002**: 로깅을 통한 디버깅 지원
- **NFR-MAINT-003**: 단위 테스트 작성

---

## 4. 제약사항

### 4.1 기술적 제약사항
- **Java 버전**: JDK 1.6.0_45 (컴파일 타겟: Java 5)
- **Spring Framework**: 2.5.6
- **PUBC 모듈**: iros_pubc_1.1.jar (외부 의존성)
- **Apache CXF**: 2.2.9 이상
- **iBATIS**: 2.3.4 (PUBC 요구사항)

### 4.2 환경적 제약사항
- **WAS**: Apache Tomcat 6.0 이상
- **데이터베이스**: PUBC 지원 DBMS (Oracle, Sybase, PostgreSQL 등)
- **운영체제**: Cross-platform (Windows, Linux, macOS)

### 4.3 비즈니스 제약사항
- PUBC 모듈의 표준 인증 프로세스 준수 필수
- 공공데이터 포털 연계 표준 준수
- 서비스키 발급은 외부 시스템에서 관리 (본 프로젝트 범위 외)

---

## 5. 전제조건

### 5.1 PUBC 모듈 사전 설정
- `iros_pubc_1.1.jar` 파일 확보
- PUBC 데이터베이스 스키마 구축
  - `TN_PUBC_USE_STTUS_INFO`: 사용 현황 테이블
  - `TN_PUBC_USER_CRTFC`: 인증 정보 테이블
  - 기타 PUBC 필수 테이블
- `pubc_common_properties.xml` 설정 파일 준비

### 5.2 개발 환경
- Eclipse IDE
- JDK 1.6.0_45 설치
- Maven 빌드 도구
- 데이터베이스 클라이언트 (SQL Developer, DBeaver 등)

### 5.3 테스트 데이터
- 테스트용 서비스키 발급
- 샘플 사용자 데이터
- IP 화이트리스트 설정

---

## 6. 우선순위

### 6.1 필수 기능 (P0 - 1차 개발)
- FR-AUTH-001: 서비스키 인증
- FR-LOG-001: 사용 현황 로깅
- FR-REST-001: REST 문화시설 목록 조회 API
- FR-SOAP-001: SOAP 문화시설 목록 조회 서비스

### 6.2 중요 기능 (P1 - 2차 개발)
- FR-REST-002: REST 문화시설 상세 조회 API
- FR-SOAP-002: SOAP 문화시설 상세 조회 서비스
- FR-AUTH-002: IP 검증
- FR-LOG-002: 장애 정보 로깅

### 6.3 선택 기능 (P2 - 추후 개발)
- FR-LOG-003: 통계 정보 제공
- FR-AUTH-003: 사용량 임계값 검증
- 관리자 웹 콘솔

---

## 7. 사용자 스토리

### US-001: API 개발자로서 문화시설 목록을 조회하고 싶다
**Given**: 유효한 서비스키를 발급받은 상태
**When**: REST API `/api/cultureFacilities?serviceKey=xxx&regionCode=11`를 호출하면
**Then**: 서울지역 문화시설 목록이 JSON 형식으로 반환되고, 호출 내역이 자동으로 로깅된다

### US-002: 일반 사용자로서 특정 박물관의 상세 정보를 확인하고 싶다
**Given**: 유효한 서비스키를 가진 상태
**When**: REST API `/api/cultureFacilities/F001?serviceKey=xxx`를 호출하면
**Then**: 해당 박물관의 주소, 전화번호, 운영시간, 위치 좌표 등 상세 정보가 반환된다

### US-003: API 개발자로서 잘못된 서비스키 사용 시 명확한 오류를 받고 싶다
**Given**: 등록되지 않은 서비스키를 사용
**When**: REST API를 호출하면
**Then**: "서비스키가 등록되지 않았습니다" 오류 메시지와 함께 HTTP 401이 반환되고, 장애 정보가 로깅된다

### US-004: SOAP 클라이언트 개발자로서 WSDL을 통해 서비스를 이해하고 싶다
**Given**: SOAP 웹 서비스가 배포된 상태
**When**: `http://host:port/services/CultureFacilityService?wsdl`에 접근하면
**Then**: 자동 생성된 WSDL 문서가 반환되어 서비스 구조를 파악할 수 있다

### US-005: 시스템 관리자로서 API 호출 내역을 추적하고 싶다
**Given**: API가 운영 중인 상태
**When**: 데이터베이스의 `TN_PUBC_USE_STTUS_INFO` 테이블을 조회하면
**Then**: 모든 API 호출 내역(성공/실패)이 타임스탬프와 함께 기록되어 있다

---

## 8. 인수 기준 (Acceptance Criteria)

### 8.1 기능적 인수 기준
- [ ] 모든 REST API가 서비스키 인증 후 정상 동작 (조회 전용)
- [ ] 모든 SOAP 서비스가 WSDL을 통해 정의되고 호출 가능 (조회 전용)
- [ ] 인증 성공/실패 모두 자동 로깅
- [ ] 표준 에러 코드 응답 (PUBC 에러 코드 체계 준수)
- [ ] JSON 및 XML 응답 포맷 지원
- [ ] 페이징 기능 정상 동작 (pageNo, numOfRows)
- [ ] 검색 필터 기능 정상 동작 (시설 유형, 지역 코드, 시설명)

### 8.2 비기능적 인수 기준
- [ ] API 평균 응답 시간 1초 이내
- [ ] 동시 50명 접속 시 안정적 동작
- [ ] 단위 테스트 커버리지 70% 이상
- [ ] 코드 컨벤션 준수 (주석 포함)
- [ ] 설치 및 설정 가이드 문서 제공

---

## 9. 위험 및 대응 방안

### 9.1 기술적 위험

#### 위험 1: PUBC 모듈 버전 호환성 문제
- **발생 확률**: 중
- **영향도**: 높음
- **대응 방안**:
  - PUBC Ver.2.3에 명시된 기술 스택 준수
  - 통합 테스트 시 PUBC 인증/로깅 검증 철저히 수행

#### 위험 2: 레거시 기술 스택 (Java 5/6, Spring 2.5.6)
- **발생 확률**: 높음
- **영향도**: 중
- **대응 방안**:
  - 최신 IDE에서 JDK 1.6 호환 모드 설정
  - 레거시 라이브러리 의존성 명확히 관리

#### 위험 3: CXF SOAP 서비스 동적 생성 복잡도
- **발생 확률**: 중
- **영향도**: 중
- **대응 방안**:
  - OASIS 소스코드 참조
  - 단계별 개발 (REST 먼저, SOAP 나중)

### 9.2 일정 위험

#### 위험 4: PUBC 환경 설정 지연
- **발생 확률**: 중
- **영향도**: 높음
- **대응 방안**:
  - 샘플 설정 파일 사전 준비
  - 인메모리 DB(H2) 활용 초기 개발 진행

---

## 10. 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| 1.0 | 2025-11-03 | Claude | 초기 작성 (CRUD 기반) |
| 2.0 | 2025-11-04 | Claude | **조회 전용 API로 변경** - 문화시설 정보 조회 |

---

## 부록 A: 용어 정의

| 용어 | 설명 |
|------|------|
| **PUBC** | 공공데이터 포털의 공통연계모듈 (Public Common Module) |
| **서비스키** | API 인증을 위한 고유 식별자 |
| **UserCrtfcVO** | PUBC 사용자 인증 정보 Value Object |
| **troblTyCode** | 장애 유형 코드 (00: 정상, 01~99: 오류) |
| **UseSttusService** | PUBC 사용 현황 로깅 서비스 |
| **JAX-WS** | Java API for XML Web Services (SOAP) |
| **JAX-RS** | Java API for RESTful Web Services |
| **문화시설** | 박물관, 미술관, 도서관 등 문화 관련 공공시설 |
| **지역코드** | 행정구역 코드 (11:서울, 26:부산, 31:경기 등) |

## 부록 B: 참조 표준

- 공공데이터 포털 OpenAPI 개발 표준
- PUBC 공통연계모듈 Ver.2.3 매뉴얼
- RESTful API 설계 가이드 (HTTP 메서드, 상태 코드)
- SOAP/WSDL 1.1 표준
